import React from 'react';

/// Styles
import '../styles/ButtonContainerStyle.scss';

const ButtonContainer = ({ children }) => (
  <div className="button-container">{children}</div>
);

export default ButtonContainer;
